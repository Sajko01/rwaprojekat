services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: sajko
      POSTGRES_PASSWORD: sajko123
      POSTGRES_DB: rwaprojekat1
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data

